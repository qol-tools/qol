# qol-monorepo dead-code removal scan - CONSOLIDATED FINAL

Date: 2026-08-01. Method: two independent passes over the same 14 slices. Pass 1 was a 14-agent audit swarm (tray-rust-core, tray-ui, libs-contract, libs-build-identity, libs-system, libs-dev, plugins-picker, plugins-sessions, plugins-input, plugins-devices, plugins-misc, tools-cli, cross-cutting-deps, cross-cutting-duplication), each producing a slice report with CONFIRMED / LIKELY / UNCERTAIN verdicts backed by full-repo greps, git history and, where claimed, cargo checks. Pass 2 was a 14-agent adversarial refutation swarm: each slice was re-attacked independently with stem/case-variant greps, string/serde surfaces, same-name traps, cfg sweeps, sibling-repo greps (qol-skills, qol-migrate, kmrh47-skills, qol-tts, worktrees), `git log -S` history, and overlooked-directory sweeps (.github/scripts, .githooks, flows, .claude). **Adversarial pass is final word**: an item survives only if no adversary found a live consumer; any adversary finding (live consumer, missed citation, broken test pin, or live CI wiring) refutes or downgrades the original claim. No repo edits, no builds re-run; this file merges both passes as-is.

## Executive summary

| Slice | original CONFIRMED | final SURVIVES | DOWNGRADED | REFUTED |
|---|---|---|---|---|
| tray-rust-core | 5 | 5 | 0 | 0 |
| tray-ui | 58 | 49 | 9 | 1 |
| libs-contract | 1 | 1 | 0 | 0 |
| libs-build-identity | 4 | 4 | 0 | 0 |
| libs-system | 7 | 4 | 2 | 2 |
| libs-dev | 2 | 2 | 0 | 0 |
| plugins-picker | 2 | 2 | 0 | 1 |
| plugins-sessions | 3 | 3 | 0 | 0 |
| plugins-input | 1 | 1 | 0 | 1 |
| plugins-devices | 20 | 20 | 0 | 0 |
| plugins-misc | 7 | 7 | 0 | 1 |
| tools-cli | 1 | 1 | 0 | 0 |
| cross-cutting-deps | 4 | 4 | 0 | 1 |
| cross-cutting-duplication | 16 | 14 | 2 | 0 |
| **TOTAL** | **131** | **117** | **13** | **7** |

Campaign totals: **131 items CONFIRMED overall** (plus 70 LIKELY + 11 UNCERTAIN = 212 items audited). **192 items survive adversarial review** (117 CONFIRMED + 67 LIKELY + 8 UNCERTAIN). **20 refuted/downgraded** (7 REFUTED — proven live, must not be removed; 13 DOWNGRADED — CONFIRMED reduced to LIKELY/UNCERTAIN). REFUTED column in the table counts refutations from any original category (noted per row); CONFIRMED rows sum as SURVIVES + DOWNGRADED.

## DO NOT REMOVE - refuted items

Every item an adversary proved live. Removing any of these breaks code, tests, CI, or documented contracts.

1. **`apps/qol-tray/ui/features/task-runner/style.css` (whole file, 258 lines)** — original tray-ui UNCERTAIN claimed "never loaded", "not imported by JS", "missing `@import` bug". **REFUTED: the file IS loaded at runtime.** Live consumer: `apps/qol-tray/ui/views/task-runner-view.js:35-42,60` — `ensureCssLoaded()` injects `<link rel="stylesheet" href="/features/task-runner/style.css">` (`link.href = '/features/task-runner/style.css'` at :40) from `useEffect(ensureCssLoaded, [])`, inside `TaskRunnerView`, a registered live view (`app/views.js:8,39`). Selectors `.actions-list` (:1), `.action-card` (:9,17,21), `.test-panel` (:86), `.api-usage` (:224) match live markup (`action-list.js:8,30,40`, `task-runner-view.js:47-52`). `CSS_ID = 'task-runner-css'` guard makes the loader idempotent; `git log -S` missed it because of the leading `/features/` path form. Deleting strips live Task Runner styling.
2. **`libs/qol-process/src/lib.rs:138-140` — `ProcessTreeGuard::root_has_exited()`** — original libs-system CONFIRMED claimed zero callers ("in-crate grep -> no caller"). **REFUTED: live test consumer missed by the report's own grep.** Live consumer: `libs/qol-process/tests/process_lifecycle.rs:336` — `assert!(guard.root_has_exited().unwrap())` in `preexec_cgroup_owns_an_immediate_exit_before_assignment` (`#[cfg(target_os = "linux")]`, helper `owned_process_tree()` at :158). Test added 2026-07-17 (d751d2ee), method re-platformed 2026-07-28 (877fcc25). The linux impl (`platform/linux/containment/mod.rs:402`) exists to serve this test via the pub forwarder. Deleting breaks a hard CI failure; the four platform impls go only if the test's cgroup root-exit semantics assertion is deleted/rewritten first.
3. **`plugins/os-themes/src/theme/platform/linux/backends/kde.rs:9-16` — `Kde` backend** — original plugins-input LIKELY claimed "deleting it changes only the error message". **REFUTED: deletion breaks a pinned test and silently reclassifies a mainstream desktop.** Live consumer: `plugins/os-themes/src/theme/platform/linux/mod.rs:46-59` — `backend_detection_table` pins `("KDE", true)` at :50 against `backend_for(...).is_ok()`; removing the `DesktopEnvironment::Kde =>` arm (mod.rs:34) flips that assertion. On a real KDE session the stub's `Err("KDE theme switching is not implemented yet")` (kde.rs:11,15) is what users see; removing the arm reclassifies KDE from "classified desktop, feature pending" to "unsupported". Plan doc `docs/plans/2026-07-13-os-themes-architecture-theme-toggle.md:668,679-684` shows it as the designed "implement later" seam. (The `DesktopEnvironment::Kde` enum variant itself survives any backend deletion — it is pinned separately at desktop.rs:14,29 and doctor/platform/linux.rs:52,128.)
4. **`plugins/removeapp/src/daemon/actions.rs:16,23` — `Command::Kill` + `"kill"` wire string** — original plugins-misc LIKELY claimed "nothing in-repo sends `"kill"` to removeapp's socket". **REFUTED: `"kill"` is live daemon-replacement protocol with in-repo senders; the report's own qol-shot KEEP entry contradicts it.** Evidence chain: (a) qol-tray spawns EVERY plugin-daemon with `QOL_TRAY_DAEMON_REPLACE_EXISTING=1` (`apps/qol-tray/src/plugins/daemon_lifecycle/spawn.rs:102`); (b) removeapp is NOT in the pre-bind `MIGRATED_PLUGINS` list (`apps/qol-tray/src/plugins/daemon_lifecycle/listener/platform/unix.rs:13-23`), so its daemon self-binds via `qol-plugin-daemon::daemon::bind_listener`; (c) on `AddrInUse` with replace-existing armed, the library runs `replace_existing` → `send_kill(config)` — the literal `"kill"` — against the running daemon (`libs/qol-plugin-daemon/src/daemon/platform/unix.rs:355,377-399,158-160`) and waits for it to die; (d) removeapp's `CONFIG` declares `support_replace_existing: true` (`actions.rs:10`), opting into the contract; (e) documented at `docs/plugin-contract.md:380-401`. Mapping `"kill"` → `Fallback` makes the old daemon ignore the kill, the replacement loop hits its deadline, and the new daemon fails to bind. The `"show"` alias alone (actions.rs:22) has no in-repo sender and could be dropped separately.
5. **`plugins/qol-voice/Cargo.toml:23-28` — feature `local-stt`** (gates candle-core, candle-nn, candle-transformers, hf-hub, tokenizers) — original cross-cutting-deps UNCERTAIN claimed `--features local-stt` "appears nowhere" and the feature is "unreachable in every build". **REFUTED: the feature is compiled by CI and by every official qol-voice plugin release** — the original grep scope missed `.github/scripts` (it covered workflows only). Live consumers: `.github/scripts/release_candidate.py:26` `PLUGIN_RELEASE_FEATURES = {"qol-voice": "local-stt"}` applied at :234-236 to the release build command (:228-233), run from `.github/workflows/plugin-version.yml:179,235,278`; `.github/scripts/affected_crates.py:51` `PACKAGE_FEATURES = {"qol-voice": "qol-voice/local-stt"}` emitted into every CI invocation (feature_flags() :58-67, applied :96-102) consumed by `ci.yml:119,126,187`; `.github/scripts/tests/test_release_candidate.py:387-404` asserts the `--features local-stt` build command; `plugins/qol-voice/Makefile:44-47` `ci-local` runs `--all-features`; root `Cargo.toml:116-147` carries `[profile.dev.package.candle-*/gemm*/pulp]` opt-level entries. Feature + wiring landed together in 68298d9e. Deleting it breaks release_candidate.py / affected_crates.py (cargo errors on unknown feature) and strands the profile entries — only coordinated with a CI/script change.
6. **`plugins/launcher/src/lib.rs:15` — `pub use qol_search::{fuzzy_match, FuzzyMatch};`** — original plugins-picker LIKELY claimed "its only consumer is `tests/fuzzy_props.rs:1`". **REFUTED: consumer inventory incomplete — it is not test-only.** Live consumer: `plugins/launcher/examples/14_fuzzy_match.rs:2` (`use launcher::{fuzzy_match, open_window_with_focus, FuzzyMatch};`, used at :39,47,59), a declared `[[example]] name = "14_fuzzy_match"` target (`Cargo.toml:104-106`, `required-features = ["examples"]`). Deleting the re-export breaks `cargo build --features examples --example 14_fuzzy_match` unless the example is switched to `use qol_search::…` (same trivial fix as the test; no CI builds the examples, so impact is dev-surface).
7. **`libs/qol-terminal-sessions/src/cli/mod.rs:20-38` — `CliSessionStrategy` trait** (+ `CliTool`, `CliToolId`, `CliSessionDescriptor`) — original libs-system UNCERTAIN claimed "no crate outside libs/qol-terminal-sessions implements it today" and suggested shrinking to `pub(crate)`. **REFUTED: an external (test) implementation exists.** Live consumer: `plugins/cli-sessions/tests/reconcile.rs:44` — `impl CliSessionStrategy for FakeCodex`, and :76 builds `Arc<dyn CliSessionStrategy>`; `plugins/cli-sessions/ARCHITECTURE.md:51,121` document the per-tool registry as the designed extension point. Trait + descriptor types must stay public.

## DOWNGRADED items

CONFIRMED in the original pass, reduced by the adversarial pass. Do not delete as "confirmed dead".

- **tray-ui (9, CONFIRMED → LIKELY)** — five surface-actions factories and three dev-logging/world helpers are documented canonical API in the sibling qol-skills repo, which the original audit never scanned; deletion breaks the documented agent-facing contract until qol-skills is updated first:
  - `apps/qol-tray/ui/lib/surface-actions.js:5` `diveAction`, `:9` `openExternalAction`, `:13` `revealInFolderAction`, `:28` `copyPathAction`, `:32` `customAction` — zero in-repo consumers, but documented at qol-skills `plugins/qol-tray/skills/qol-tray-ui-systems/SKILL.md:240-263` ("Add new action kinds here, not in surfaces") + `plugins/qol-tray/agents/qol-tray-frontend.md:25`. NOTE: `copyTextAction` (original KEEP) is called ONLY by the dead `copyPathAction` — transitively dead; and `runActions` (:53) is the sole caller of KEEP'd `pickAction`/`modifierIndex` — deleting it strands those two, so the original KEEP section is internally inconsistent; handle the chain as a unit.
  - `apps/qol-tray/ui/lib/world-geometry.js:139` `inflatedEntryRange` — no in-repo consumer (last usage removed by minimap refactor cbf24ace), but documented at qol-skills `plugins/qol-tray/skills/qol-world-canvas/SKILL.md:215` as the canonical inflation formula.
  - `apps/qol-tray/ui/lib/debug.js:27` `isVerboseDebugEnabled`, `:29` `setVerboseDebugEnabled`, `:71` `pointLabel` — no in-repo consumer (the module-level `verbose` flag IS live via localStorage `qol-debug-verbose` + `log.verbose(...)` at spatial-nav.js:56, Minimap.js:389, but is mutated only via the dead setter), yet documented at qol-skills `plugins/qol-tray/skills/qol-tray-dev-logging/SKILL.md:46` (programmatic toggle) and `:27,84-87,134,144,158` (pointLabel as the canonical coordinate formatter).
- **libs-system (2)**:
  - `libs/qol-platform/src/lib.rs:23-25` `linux_display_backend()` + `:9-13` `LinuxDisplayBackend` (CONFIRMED → LIKELY) — zero in-repo consumers on every cfg (all 8 `qol_platform::` call sites use only disk_space/launch_working_dir/cinnamon::Session; last consumer `libs/qol-plugin-daemon/src/focus/*` deleted 2026-07-08 in f9acf21c, tray diagnosis restructured in 39be77d4), BUT the sibling qol-migrate repo's 2026-06-03 snapshot still contains the pre-removal consumer (`qol-migrate/mono-baked/libs/qol-plugin-daemon/src/focus/platform/linux.rs:8-9` plus fresh/ and mono-graft/ copies). Proven stale (current main has no focus/ module and no qol-platform dep), so removal is safe in-repo, but coordinate with a qol-migrate re-bake or accept drift; also update `libs/qol-platform/README.md:16-23`.
  - `libs/qol-platform/src/lib.rs:27-29` `current_capabilities()` + `:15-21` `PlatformCapabilities` (CONFIRMED → UNCERTAIN) — zero code consumers in-repo, BUT the CURRENT qol-skills (last commit 2026-07-31) `plugins/qol-project/skills/qol-arch-channels/SKILL.md:15,32` names `qol_platform::PlatformCapabilities` as the "Capability | static | Per-OS feature gate" channel ("Dynamic capability negotiation. PlatformCapabilities already covers it."). Deleting silently breaks the documented host/plugin channel architecture; the skill must be updated first and a human must confirm no capability gate is planned.
- **cross-cutting-duplication (2)** — the two clusters where "identical copies" was overstated:
  - Wire protocol pair (CONFIRMED → LIKELY): `apps/qol-tray/src/plugins/action_transport/platform/unix.rs:12-99` vs `libs/qol-plugin-daemon/src/daemon/platform/unix.rs:94-156` — the tray copy is a superset, NOT functionally equivalent: tray `parse_response` (:81-99) additionally parses legacy plain-text replies (`"handled"`/`"fallback"`/`"error ..."`, pinned by tray tests :142-176) which the lib copy maps to `Err(InvalidData)`; tray reads via raw unbounded `BufRead::read_line` (:74-79) vs the lib's bounded 64 KiB `local_ipc::read_line` — adopting the bounded read is a silent behavior change for daemon responses >64 KiB. Request framing (JSON + `\n`, `authorize_peer`, `qol_runtime::protocol`) IS identical; legacy plain-text branch is genuinely dead against current daemons (all reply JSON via `write_response`, unix.rs:542) but needs the human go-ahead to drop.
  - 12-daemon ping/kill/fallback skeleton cluster (CONFIRMED → LIKELY): the cluster is real (12 grep hits as counted), but the evidence citations are wrong: only alt-tab (`runtime/daemon.rs:5-36`) and launcher (`app/mod.rs:6-36`) actually contain the `DaemonConfig` + `start_listener`/`cleanup`/`send_kill` trio — removeapp has NO send_kill/cleanup in the crate, cli-sessions has none in Rust (only the refuted `[action.cleanup]` toml entry); qol-shot's `start_listener` embeds an extra `SHOT_DAEMON_LISTEN` probe (unix_common.rs:14-18), has no send_kill, its `parse_command` has NO fallback arm (`other => ReadResult::Command(Command::Cli(other))` at :59), and its CMD_RECV probe is NOT `#[cfg(debug_assertions)]`-gated (:48-50, unconditional in release); qol-voice's `_` arm returns `ReadResult::Fallback` (app/mod.rs:66) — only controllers returns `Error` (app/mod.rs:177), contrary to the original note. Consolidation (pre-handle ping/kill) is still feasible but needs more parameterization; a naive executor would silently drop qol-shot's `Cli` catch-all or its release-mode probe.

## Corrections to original reports (found by adversaries)

- **tray-rust-core — fix_safe evidence error**: report claimed `doctor::fix_safe` has "1 file, 1 occurrence"; `tests/shell_hook_doctor.rs:100` carries the substring in test NAME `doctor_fix_safe_installs_block_into_existing_zshrc` (body at :106 calls `fix_single`, not `fix_safe`). Last real consumer removed in 2ab316ec (qol-migrate snapshots show the historical `doctor::fix_safe()` body). Verdict unchanged — still dead, deletion safe, but re-grepping reviewers will see the substring.
- **tray-ui — blanket verification claim wrong for two rows**: "repo-wide rg -> 1 hit (the definition)" is factually false for `activateSurface` and `PluginConfigView` — stale plan-doc hits exist (`apps/qol-tray/docs/plans/2026-04-05-world-layers.md:1068,1072`; `specs/2026-04-04-contextual-sidebar-design.md:68` + plan :265, the latter feature explicitly REMOVED). Immaterial to liveness. Also: the original never scanned sibling repos, which is where the 9 documented exports (DOWNGRADED) live. PluginRow.js correction: deletion does NOT break `lib/stray-interactive-elements.test.js:13` (the test only scans existing files; a deleted entry becomes inert); real caveat is `docs/plans/2026-07-16-tray-theming.md:737,882` naming the file.
- **tools-cli — "working tree does not compile" is a false claim**: `cargo check -p qol` passes on the committed tree (verified via `git archive` HEAD export + isolated `--target-dir`; zero warnings). Also: the cited compile-proof artifacts (`check-noplatform.log`, `head-check/`, `check-target/`, `check-qol*.log`) DO NOT EXIST in `target/removal-scan/` — the "removal compile-proven" claim is unverifiable as written; and the parenthetical "0 hits anywhere outside libs/qol-platform itself" is false (qol-platform has live consumers in qol-apps, qol-dev-env, qol-shot, window-actions, launcher — only the `qol` crate edge is dead). The CONFIRMED finding itself stands.
- **plugins-picker — launcher fuzzy_match consumer inventory**: not test-only; `examples/14_fuzzy_match.rs:2` also consumes it (see REFUTED #6).
- **plugins-devices — date**: the znp target-inventory scaffolding landed 2026-03-24, not 03-15 (git evidence). Immaterial.
- **cross-cutting-deps — missed .github/scripts**: the `local-stt` dormancy evidence is wrong; `--features local-stt` appears in `release_candidate.py:26,234-236`, `affected_crates.py:51,96-102`, and the qol-voice `Makefile:44-47` (see REFUTED #5). Also missed: root `Cargo.toml:116-147` candle profile entries.
- **cross-cutting-duplication — skeleton citations**: see DOWNGRADED — "Seven of them repeat the same DaemonConfig const + start_listener/cleanup/send_kill trio with identical bodies (read-verified: ...)" holds for only alt-tab + launcher; the removeapp/cli-sessions pair contains no such trio (the removeapp≡cli-sessions `parse_command` item itself SURVIVES with accurate citations).
- **plugins-input — Kde removal-impact claim**: "deleting it changes only the error message" is factually wrong (breaks `backend_detection_table`, see REFUTED #3). Mechanical caveat on the CONFIRMED animation guard: `factor` is used at :16-17 (`scaled_dimension(base.width, factor)`), so removal must delete only the `if factor <= 0.0 { return None; }` block (:13-15) and KEEP `let factor = scale;` (:12), or inline `scale`; deleting :12-15 verbatim would not compile.

## Prerequisites before deleting

1. **Update qol-skills first** (sibling repo, not this one): `qol-arch-channels/SKILL.md:15,32` references `qol_platform::PlatformCapabilities` (libs-system downgrade); `qol-tray-ui-systems/SKILL.md:240-263` documents the 5 surface-actions factories; `qol-tray-dev-logging/SKILL.md:27,46,84-87,134,144,158` documents `pointLabel` + the verbose toggles; `qol-world-canvas/SKILL.md:215` documents `inflatedEntryRange`. All 9 tray-ui exports are documented there and must be re-documented or un-documented before removal.
2. **Re-check qol-migrate sibling for stale references**: its 2026-06-03 baked snapshots (mono-baked/, fresh/, mono-graft/) still contain pre-removal consumers (`linux_display_backend` in `qol-migrate/mono-baked/libs/qol-plugin-daemon/src/focus/platform/linux.rs:8-9`, qol-platform `PlatformCapabilities` export, the old `doctor::fix_safe()` test body at mono-baked/.../tests/shell_hook_doctor.rs:107, identical dead lights/znp files). Proven stale, but `bake-and-verify.sh` tree-verifies against main — coordinate a re-bake or accept drift.
3. **qol-voice local-stt**: any change must be coordinated with `.github/scripts/release_candidate.py`, `.github/scripts/affected_crates.py`, `plugins/qol-voice/Makefile:44-47`, and root `Cargo.toml:116-147` (see REFUTED #5).
4. **window-actions saved_rect**: before changing the state-file format check the qol-tray-data-migrations policy; `parse_record` (state_store.rs:101) already treats the `|x,y,w,h` suffix as optional, so old files keep parsing, but the on-disk record format is the real gate.
5. **tray-ui surface-actions chain**: `runActions` deletion leaves KEEP'd `pickAction`/`modifierIndex`/`copyTextAction` transitively dead — execute the chain as one unit and fix the original KEEP list.
6. **os-themes Kde**: any backend removal must explicitly update `backend_detection_table` (linux/mod.rs:46-59) and make the product decision the plan doc records.

## Suggested execution order

1. **Five dead dependency edges first** (each: delete line → `cargo check` + relevant tests):
   - `tools/qol-cli/Cargo.toml:29` — `qol-platform.workspace = true` (CONFIRMED; also in cross-cutting-deps)
   - `libs/qol-dev-guest/Cargo.toml:14` — `tempfile = "3.8"` dev-dep (CONFIRMED)
   - `libs/qol-conventions/Cargo.toml:12` — `toml` (workspace) unused (CONFIRMED; alternative: rewrite the hand-rolled mini-parser on top of it — see DUPLICATION)
   - `Cargo.toml:41` — workspace `qol-tray` entry (CONFIRMED; crate stays a member)
   - `Cargo.toml:29` — workspace `qol-wasm` entry (CONFIRMED; crate + bundle stay)
2. **Then per-slice CONFIRMED survivors**, smallest blast radius first: tray-rust-core (5), libs-dev (2), tools-cli deps (done), plugins-devices (20, all mechanical), plugins-misc (7), plugins-picker (2), libs-contract (1), libs-build-identity (4), cross-cutting deps (done). Each item: remove → `cargo check -p <crate>` → run that crate's tests → full-repo grep to confirm zero residual references. For libs-system only the 4 SURVIVES (`as_exit_code`, `DoctorCheck::about`, `PlainTextOutput` From impls, `BackendId::as_str`); skip the 2 downgraded + root_has_exited until prerequisites 1-2 are done.
3. **Never touch** the 7 REFUTED items, the 13 DOWNGRADED items, or the 8 surviving UNCERTAIN items (dark_css/Profile::Core, windows-adapter cluster, observe_input, set_activation/select_terminal_target, DeviceLeft, saved_rect chain, CGEventTap, socket-path 4x) without the listed human checks.

## Per-slice final lists (SURVIVED items)

### 1. tray-rust-core (5 CONFIRMED / 12 LIKELY — all survive)
- `apps/qol-tray/src/dev_generation/mod.rs:140` `release_daemon_autostart()` — sets `DAEMON_AUTOSTART_RELEASED=true`, never called; production path `promote_to_stable()` (mod.rs:144, live via `features/plugin_store/server/mod.rs:140`). Search: `release_daemon_autostart` → 1 hit (declaration); `daemon_autostart_held` read side live (app/mod.rs:514, plugins/daemon_health.rs:50, manager/runtime.rs:166, daemon_supervisor.rs:66). Adversary: git history shows never called after introduction (7bd65470/784173c7).
- `apps/qol-tray/src/doctor/mod.rs:74` `fix_safe()` — wrapper for `fix_with_policy(FixPolicy::safe())`, zero callers; cli.rs:168 inlines the policy directly. Search: `fix_safe` → 1 occurrence + test-name substring (see Corrections); `fix_single`/`check_quick`/`check_single` siblings live.
- `apps/qol-tray/src/commands/mod.rs:33` `deeplink_url(route, port)` — superseded by `local_http::browser_url` (local_http.rs:22, live at app/mod.rs:259,280,657; third near-copy at server/security.rs:27). Search: `deeplink_url` → declaration + same-file tests only; git: replaced by token-bearing browser_url (5d21f598, ec473276).
- `apps/qol-tray/src/dev/runtime_gpui.rs:30` `GpuiRuntimeConfig::set_ghost_opacity()` — HTTP persist path mutates the field directly (dev_state_handlers.rs:87-88). Search: `set_ghost_opacity` → 1 hit. (`qol_gpui::popup_window::set_ghost_debug` is a DIFFERENT symbol — same-name trap cleared.)
- `apps/qol-tray/src/dev/runtime_gpui.rs:36` `GpuiRuntimeConfig::set_ghost_debug_color()` — same pattern; dev_state_handlers.rs:90-91 assigns via `normalize_ghost_debug_color` (live re-export, dev/mod.rs:24). Search: `set_ghost_debug_color` → 1 hit.
- LIKELY (12, no refutation): `installer/mode.rs:51` `ModeConfig::is_prod()` (test-only; live sibling `is_dev()` via dev_gate.rs:16); `paths/mod.rs:172` `set_active_profile_name()` + `:182` `profile_active_dir()` + `:194` `profile_core_dir()` + `:198` `profile_os_dir()` + `:202` `profile_device_dir()` (all test-only; superseded by `ProfileScopeStore`, read side `active_profile_name()` live at startup.rs:5, scope_store.rs:43, core/storage.rs:8; no profile-switching plan in docs/skills); `features/profile/registry.rs:14` `list_profile_names()` + `:29` `create_profile()` + `:42` `delete_profile()` (test-only; production via `ensure_profile_dirs_for()`); `dev/boot_contract.rs:118` `InMemoryWorktreeLister` + `:130` `InMemoryBinaryProbe` (test doubles, same-file `#[cfg(test)]` only; integration uses real lister/probe); `logging/probe.rs:1` + `src/lib.rs:32` probe re-export (all 71 `probe!` sites use `qol_runtime::probe!` directly; contrast case `qol_gpui::probe::probe` IS consumed at alt-tab/app/render.rs:102 — the tray copy has no such consumer; `console_guard` stays, live via app/mod.rs:32). Search terms as in original.

### 2. tray-ui (58 CONFIRMED → 49 survive, 9 downgraded, 1 refuted; 4 LIKELY survive)
- Whole modules (3): `views/dev/cpu/sparkline.js:8` `renderCpuSparkline` (0 importers; CSS classes `plugin-cpu-sparkline*` belong to live inline SVG in `views/dev/components/CpuStrip.js:26-28`; usage removed by 456e9f3a); `lib/components/ShortcutLegendPreact.js:3` `ShortcutLegend` (0 importers; superseded by live `KeyLegend`); `lib/hooks/useHashSubPath.js:10` `useHashSubPath` (0 importers; import+call removed in eaeb412f).
- Dead exports (16 survive): `lib/surface-actions.js:53` `runActions` (not documented in qol-skills — sole outlier of the 6; but see prerequisite 5: its callees `pickAction`/`modifierIndex` become transitively dead); `lib/surface-traits.js:7` `activateSurface` (0 importers among the 8 module importers; usage removed in e9fb79e3); `views/store/reducer.js:4` `createStoreState` + `:26` `normalizeSearchQuery` + `:35` `clampSelectedIndex` (alias-only; live `clampIndex` used at :43); `utils/escape-html.js:11` `escapeHtml` (live `safeStatusToken:16` never calls it; last call deleted e8b3bd65); `views/plugin-config/field-map.js:40` `SUPPORTED_FIELD_KINDS` (`FIELD_MAP` live at :46); `views/dev/mock/reducer.js:77` `hasActiveBackendTargets` + `:101` `pruneInactiveTargets` (module live via 8 other exports); `features/auth/actions.js:7` `hasInsufficientScope` (importers take `fetchAuthHealth`/`insufficientScopeIssue` only); `fx/dissolve/index.js:54` `materializeInScatter` + `:120` `materializeIn` (module live via `dissolveIn`/`DISSOLVE_PRESETS`; created 24d9cb71, never consumed); `lib/hooks/useGridNav.js:37` `useListNav` (`useGridNav` live; `navigateGrid` internal :6); `views/plugin-config/view.js:34` `PluginConfigView` (live export `PluginConfigSectionView:70`; stale plan-doc hits only — see Corrections); `views/profile/summary.js:74` `profileRemoteSummary` (0 consumers among 5 importers); `lib/hooks/useIdleHide.js:20` `useViewportHide` (alias; importers use `useOverlayHide`). Search terms as in original.
- CSS tokens (30 survive): `styles/theme-tokens.css` 29 tokens — `--accent-glow`(136) `--layer-paper-02`(90) `--layer-paper-13`(98) `--layer-paper-14`(99) `--layer-paper-22`(102) `--layer-paper-100`(105) `--amber-300`(40) `--amber-400`(39) `--amber-600`(37) `--amber-700`(36) `--blue-300`(22) `--blue-600`(19) `--blue-700`(18) `--bp-dev-md`(227) `--bp-dev-sm`(228) `--bp-dev-xs`(229) `--fs-caption-2-plus`(288) `--green-600`(25) `--layer-ink-10`(331) `--layer-ink-12`(332) `--offset-menu-trigger-right`(263) `--pad-dev-tag`(224) `--pad-token-btn`(223) `--red-400`(33) `--red-700`(30) `--size-grid-card-max`(235) `--space-0`(193) `--hairline`(334) `--hairline-strong`(335) — zero repo-wide `var()`/readVar/JS/HTML hits; look-alikes refuted individually (`var(--accent-glow-soft/-faint)` are different tokens; `--qol-tray-*` are the generated source namespace they feed FROM); plus `--selected-surface-wedge-max-glide` defined in BOTH `styles/app-shell.css:423` and `styles/common-controls.css:745` (conflicting values 120 vs 56; live `readVar` list at `lib/components/SelectionCursorOverlay.js:255-273` covers the 8 sibling tokens, not max-glide). Dynamic-read sweep of every `readVar`/`getPropertyValue`/`setProperty` call confirms none read any of the 30.
- LIKELY (4, no refutation): `components/domain-rows/PluginRow.js` (whole file; 0 importers; string hit only `lib/stray-interactive-elements.test.js:13` — deletion does NOT break that test, see Corrections; real caveat is plan doc 2026-07-16-tray-theming.md:737,882); `_mockup-profile.html` (694 lines, zero refs, fully standalone — only a human opening it directly would notice); `styles/common-config-primitives.css` `--cfg-accent-bg-hover`(12) `--cfg-muted-border`(21) `--cfg-panel-bg`(15) (external plugin-template caveat theoretical, unverified); `styles/theme-tokens.css` `--elevation-1`(156) `--elevation-2`(157) `--overlay-deep-rgb`(49) `--overlay-surface-rgb`(48) `--size-sidebar`(231) (planned-but-unlanded theming surface; design doc :57, plan :936).

### 3. libs-contract (1 CONFIRMED / 4 LIKELY — all survive)
- `libs/qol-plugin-daemon/src/activation/mod.rs:3` (+ `activation/platform/{mod,linux,macos,windows,fallback}.rs`, 65 lines) whole module — macOS accessory-policy helper with zero callers; superseded by `qol_gpui::platform::set_accessory_policy` (10 live callers; `libs/qol-gpui/Cargo.toml` has NO qol-plugin-daemon dep, so no delegation possible). Search: `qol_plugin_daemon::activation|activation::set_accessory_policy` → only docs/simplification-findings.md:23. Bonus: `objc2-foundation`/`objc2-app-kit` (`Cargo.toml:15-18`) die with it. Adversary: created wholesale in 39be77d4; the last real caller (launcher) appears only in frozen pre-refactor snapshots (qol-migrate/mono-baked + qol-monorepo.dirty.bak), migrated to gpui.
- LIKELY (4, no refutation): `libs/qol-runtime/src/broker/path/mod.rs:69-88` (+ platform/{mod,unix,fallback}.rs) broker listener half (`broker_socket_path_for_uid`, `broker_socket_path`, `bind_broker_listener`, `BrokerListener`, `BrokerPathError`) — only structural tests + Proposed ADR RUNTIME-1 (its cited design spec no longer exists); introduced dead in 670862fc. Keep `peer_cred`/`is_same_uid` (live via local_ipc/platform/unix.rs:22-23) and `PaneField`. `libs/qol-runtime/src/broker/field.rs:54` `check_field_access` + `FieldCheckError` — same unwired ADR design; dies with two structural tests. `libs/qol-plugin-api/src/restore.rs:25,37,55` + `capability.rs:27,57` + `libs/qol-runtime/src/pane_field.rs:23` restore-rule/pane-fields/launcher-provider cluster (~170 lines) — zero consumers on both contract sides; designed consumer `plugins/plugin-kitty` REMOVED 2026-06-02 (86305fa5, 31 refs); string surfaces `restore-rule`/`launcher-provider`/`pane-fields` zero in plugin.tomls; MOST DANGEROUS LIKELY item — public cross-crate contract surface (`qol_runtime::PaneField` re-export lib.rs:18), still-present Proposed ADR; delete only as a deliberate "flow is dead" decision. `libs/qol-plugin-api/src/manifest/mod.rs:41` `walk_menu_items` — last caller deleted 2026-03-12 in 6e236068 (stub_apps); `MenuItem` itself LIVE (tray re-export plugins/mod.rs:25, constructed hotkeys/catalog.rs:54-73) — remove only the walker.

### 4. libs-build-identity (4 CONFIRMED / 6 LIKELY + 1 UNCERTAIN — all survive)
- `libs/qol-gpui/src/settings_panel/mod.rs:297` `request_hosted_settings(plugin_id)` — never called; live activation paths are `open_plugin_settings`/`run_plugin_settings`/`prepare_from_async`/`SettingsWindowHost::activate_prepared`; last real caller (qol-voice `send_settings_action`) deliberately removed in f6fdacb2; same-name trap cleared (`ResolvedAction.hosted_settings` is a different symbol). Search: `request_hosted_settings` → definition only. `run_action` stays live via `SettingsRuntime::tray`.
- `libs/qol-gpui/src/settings_panel/mod.rs:224` `SettingsRuntime::with_action` — builder never invoked; `\.with_action\(` over all *.rs → zero; all 8 `SettingsRuntime` construction sites chain only `new`/`empty`/`tray` + `with_input_action`/`with_input_action_result`/`poll_query_every`. Introduced d28d6a83, never called.
- `libs/qol-artifact/src/verification.rs:97` `ArtifactExpectation::sandbox` — zero call sites; `\.sandbox\(` sweep → only `BuildIdentityEnvironment::sandbox` (different symbol); every `ArtifactExpectation::` use site enumerated → only `production`/`development_debug`/`development_release`/`with_*`.
- `libs/qol-artifact/src/verification.rs:141` `forbid_feature` — zero call sites incl. tests; keep the `forbidden_features` field + `verify_identity` check (:320-324, populated by `production()` :45). Introduced 2ec02fd9, never called.
- LIKELY (6, no refutation): `libs/qol-theme/src/lib.rs:1041-1081` six `*_dark()` accessors (`launcher_dark`, `cli_sessions_dark`, `remove_app_dark`, `shot_selector_dark`, `shot_preview_dark`, `toast_dark`) — test-only (tests/theme.rs), all `*_runtime()` counterparts live; `alt_tab_preview_plane_dark` NOT in the group (feeds css.rs:156, pinned). `libs/qol-artifact/src/verification.rs:136` `require_feature` — single test call (verification.rs:456). `libs/qol-build-identity/src/environment.rs:110` `intent()` — test-only (:340,366). `libs/qol-theme/src/css.rs:291,505-565` 13 CSS-variable push lines (`--qol-system-info-rgb`, `--qol-minimap-*`, `--qol-dissolve-target-color`, `--qol-config-qr-*`, `--qol-config-live-color-fallback`, `--qol-config-color-thumb-*`) — `var(--qol-...)` consumption sweep → zero; backing struct fields NOT dead (feed `tray_theme_js()` JS exports); delete only the push lines + regenerate. `libs/qol-theme/src/lib.rs:16` + `css.rs:201` `DEV_ACCENT_KEY`/`QOL_DEV_ACCENT` — asserted by tests/theme.rs:420, consumed nowhere; siblings `QOL_ACCENT_PRESETS`/`QOL_DEFAULT_ACCENT*` live. `libs/qol-apps/src/lib.rs:7` `read_macos_app_bundle` + `desktop/mod.rs:53` `parse_desktop_entry_content` — visibility/re-export trims only, functions must stay.
- UNCERTAIN (no refutation): `libs/qol-theme/src/css.rs:12` + `bin/qol-theme-css.rs:210` `dark_css()`/`Profile::Core` — no in-repo consumer but the flagless CLI default (`qol-theme-css --write <path>`) renders core CSS; externally visible CLI contract; human judgment required.

### 5. libs-system (7 CONFIRMED → 4 survive, 2 downgraded, 1 refuted; 3 LIKELY survive, 1 UNCERTAIN refuted)
- `libs/qol-headless/src/lib.rs:311-313` `Execution::as_exit_code()` — Search: `as_exit_code` → single hit (definition); internal code uses `ExitCode::from` directly; last external uses removed by 8fc457cc/3ba6c3dd. `ExitCode` import stays.
- `libs/qol-headless/src/doctor/check.rs:26-28` `DoctorCheck::about()` — `about()`/`\.about\(\)` zero hits; field `pub(crate)`, read at check.rs:40 + lib.rs:756. `DoctorCheck::id()` stays.
- `libs/qol-headless/src/lib.rs:47-63` `impl From<()> / From<String> / From<&str> for PlainTextOutput` — `PlainTextOutput::from` zero hits; all ~50 consumer sites construct via `text()`/`empty()`; `From<()>` additionally unreachable under `run_plain_text` handler.
- `libs/qol-terminal-sessions/src/model.rs:23-25` `BackendId::as_str()` — zero callers; every `.as_str()` resolves to `CliToolId::as_str` (cli/model.rs:20, same-name trap cleared via session/tool.rs:16, diagnostics/snapshot.rs:55); `#[serde(transparent)]` never touches it; model.rs is new code (09ba3e67, 2026-07-26).
- LIKELY (3, no refutation): `libs/qol-host-fixes/src/takeover/mod.rs:74-80` `takeover::restore()` (test-only mod.rs:99,137,151; bluetooth `restore_claimed_managers` = `restore_manager` + `takeover::clear`, never `restore()`); `libs/qol-headless/src/lib.rs:15` `EXIT_RUNTIME_ERROR` (only caller `Execution::runtime_error` :307, itself internal-only; external plugins call `CommandResult::runtime_error` — different type); `libs/qol-terminal-sessions/src/cli/builtins/mod.rs:11-29` `GENERIC_TOOL_ID`, `claude_tool()`, `generic_tool()` (in-crate only; siblings `codex_tool()`/`CODEX_TOOL_ID`/accents live via cli-sessions session/tool.rs:3,27).
- DOWNGRADED + REFUTED: see sections above (`linux_display_backend` → LIKELY, `current_capabilities`/`PlatformCapabilities` → UNCERTAIN, `root_has_exited` → REFUTED, `CliSessionStrategy` → REFUTED).

### 6. libs-dev (2 CONFIRMED / 7 LIKELY — all survive)
- `libs/qol-dev-guest/src/lib.rs:294` `GuestControlClient::connect` — delegates to `connect_cancellable`; `qol_dev_guest` crate-path sweep (25 hits) shows all consumers call `connect_verified_identity`/`_cancellable` variants (qol-cli control.rs:143, dev_session.rs:71, workflow/desktop/platform/linux.rs:395, image_import/probes.rs:64); `git log --all -S 'GuestControlClient::connect('` → NOTHING — never had a call site in all of history (added 0c982675, 2026-07-16). `connect_cancellable` stays.
- `libs/qol-dev-env/src/payload/mod.rs:146` `create_read_only_iso` — zero call sites incl. tests; all callers use `create_read_only_iso_with_runner` (qol-cli dev_bundle/mod.rs:105, flow.rs:2731); `verify_payload` stays live through the `_with_runner` variant. Git: one commit (0c982675), occurrence count never changed.
- LIKELY (7, no refutation): `libs/qol-dev-orchestrator/src/handle/run.rs:88` `RunHandle::wait` (test-only handle/tests.rs:377,418,434,449,619; production uses `poll`/`wait_timeout`/`wait_with_cancellation`; RISKIEST removal: generic name collides with `TrayHandle::wait`/`Child::wait`; qol-skills qol-dev-environments documents the TYPE, not the method) + `:115` `RunHandle::detach` (test-only :636,688; no `impl Drop` on RunHandle); `libs/qol-dev-build/src/service/mod.rs:94` `build_linked_plugins` + `:81` `build_linked_plugins_with_progress` + `:64` `build_linked_plugins_with_core_events` + `libs/qol-dev-build/src/types.rs:45` `PluginBuildProgress` (dead 4-symbol chain, only re-exports at lib.rs:19-22 + apps/qol-tray/src/dev/mod.rs:20-22; live path `default_build_application_service`/`runner::run_build` at qol-cli commands/dev.rs:314, tray dev_services/reload.rs:131, doctor/diagnosis/mod.rs:276); `libs/qol-dev-env/src/cancellation.rs:51` `clear_cancellation_request` (only orchestrator test handle/tests.rs:668; active clearing is `CancellationInbox::drop`; `request_cancellation` stays live).
- UNUSED DEP: qol-dev-guest → `tempfile` dev-dep (Cargo.toml:14) — see execution order.

### 7. plugins-picker (2 CONFIRMED / 2 LIKELY → 1 survive, 1 refuted; 1 UNCERTAIN stands)
- `plugins/alt-tab/src/discovery/mod.rs:8` `WindowInfo.preview_path: Option<String>` (+ only reader `app/render.rs:615` `img(PathBuf)` branch, pass-through render.rs:435,592) — 6 producers ALL set `None` (`discovery/platform/linux/mod.rs:383`, `macos/mod.rs:48,61`, `picker/gather.rs:545`, `state.rs:419,580`, `run.rs:377`); `WindowInfo` derives only Debug, Clone (no Default) so the producer list is exhaustive; `git log -S'preview_path: Some'` → zero commits ever; no serde derives, no config/plugin.toml key. Remove field + 6 initializers + branch.
- `plugins/launcher/tests/property_tests.proptest-regressions` (7 lines, seed `cc e5219e… # shrinks to item_count = 9, down_presses = 1`) — no `property_tests.rs` binary exists (tests/ has only `common/mod.rs`; no `[[test]]`), so proptest's same-stem auto-load can never read it; source split away in 65db674d/75c8686a; sibling `async_props.proptest-regressions` correctly pairs with the live `async_props.rs` binary and stays.
- LIKELY survivor: `plugins/launcher/src/lib.rs:10` `pub use app as daemon;` — zero consumers; every launcher-internal `daemon::` hit is `qol_plugin_daemon::daemon`; the alias pattern is repo-wide convention-dead (controllers lib.rs:7-9, qol-shot lib.rs:8; lights' `crate::daemon` is a real `pub mod`); external `qol-tools/plugin-*` repos are 404 (GitHub-verified) — the external-consumer caveat is speculative; `app` stays reachable via `pub mod app`.
- UNCERTAIN (no refutation): Windows adapter cluster, 9 files ~170 lines (alt-tab actions/capture/discovery/picker/preview_plane/monitor_listener + launcher discovery/click_away/launch windows modules) — manifests declare `platforms = ["linux","macos"]` only; CI corroborates: `.github/scripts/plugin_matrix.py:47-57` derives the release matrix strictly from plugin.toml `platforms`; no repo-owned build path compiles them. Human decision: deliberate degraded-but-compiling Windows story vs deletable scaffolding.

### 8. plugins-sessions (3 CONFIRMED / 5 LIKELY + 2 UNCERTAIN — all survive)
- `plugins/cli-sessions/src/ui/mod.rs:56` `SessionsView::dismiss` — `\.dismiss\(` hits only same-name different types (qol-gpui SurfaceDismisser, qol-shot Preview, alt-tab app); view constructed only at run.rs:166; escape path calls `dismiss_with_reason` directly (render.rs:425); `git log -S '\.dismiss()' -- plugins/cli-sessions` → zero commits.
- `plugins/cli-sessions/src/diagnostics/anomaly.rs:84` `AnomalyRecorder::forget` — `forget` grep: only definition + unrelated `std::mem::forget` + prose; `observe`/tests call only `note`; the `Recorder` wrapper never calls it.
- `plugins/cli-sessions/qol-runtime.toml:6` `[action.cleanup]` — dangling runtime-contract declaration; `parse_command` handles ping/open/show/next/snapshot/kill only; cross_validate.rs is one-directional (config fields → runtime declarations), so deleting cannot break `qol-config validate`; host iterates only `runtime_contract.queries` (unix_common.rs:344-347); no socket sender can emit "cleanup". MOST DANGEROUS LIKELY-adjacent: only removal with no compile-time protection (a Toml surface) — silent break if an external tool ever dispatches it by name.
- LIKELY (5, no refutation): `ui/selection.rs:22` `Selection::anchored()` (test-only tests/selection.rs:180,183; qol-gpui dropdown.rs:163 is a different anchored()); `session/service/mod.rs:12` `NoServiceProbe` (test fixture, 14 sites in tests/reconcile.rs; ARCHITECTURE.md:146 documents it as such); `config/mod.rs:13` `service_commands: Vec<String>` (read at run.rs:62, doctor/mod.rs:106 but no writer — absent from qol-config.toml; host merge-on-write preserves hand-edited config.json); `daemon/actions.rs:21` `"show"` alias (no sender; launcher/alt-tab/removeapp send "show" to their own sockets); `plugins/qol-voice/src/transcribe/mod.rs:49` `TranscriberRequest::automatic()` (sole caller the `#[cfg(feature = "local-stt")]` test at :214).
- UNCERTAIN (2, no refutation): `plugins/qol-voice/src/voice_session/mod.rs:193` `observe_input` cluster (VoiceSessionInput :36, ControlRequest::ObserveInput :175, VoiceSession::observe_input :346) — no in-repo caller; qol-tts fork contains the same dead API with zero internal callers either; external-consumer risk speculative, human check required. `plugins/qol-voice/src/app/mod.rs:63,64` `set_activation`/`select_terminal_target` — declared in qol-runtime.toml but NO plugin.toml `[action.*]` binding (only start_listening/stop_listening/settings), so the host action executor cannot dispatch them; declared-contract risk stands.
- Dep hygiene (not removal): `plugins/ide-checkout` `qol-conventions` used only by build.rs + `#[cfg(test)]` code (daemon/server.rs:459,463,476) — could move to `[dev-dependencies]`.

### 9. plugins-input (1 CONFIRMED / 2 LIKELY → 1 survive, 1 refuted)
- `plugins/os-themes/src/cursor/platform/linux/display/x11/animation.rs:12-15` unreachable duplicate guard (`if factor <= 0.0` after `!scale.is_finite() || scale <= 0.0` at :9, with `factor = scale` at :12) — line 9 dominates line 13; only entry points session.rs:70/298, same cfg gate; predates split commit 5c98a83b. Search: `factor` (only this fn), `scale <= 0.0`. MECHANICAL CAVEAT: keep `let factor = scale;` (:12) — `factor` is used at :16-17 — delete only the `if factor <= 0.0 { return None; }` block (:13-15).
- LIKELY survivor: `plugins/controllers/src/lib.rs:7-9` re-export aliases `pub use app as daemon; pub use detection as detect; pub use fixes::{apply, state};` — `plugin_controllers::` has exactly 4 repo hits (main.rs:6,14 + unchecked plan doc 2026-07-08-plugin-controllers.md:1116,1124 whose `daemon::run_from_env()` form was never adopted); in-crate `apply::apply` at app/mod.rs:328 resolves via `use crate::fixes::apply;` (app/mod.rs:8), NOT the re-export; external `qol-tools/plugin-controllers` is 404. All four names remain reachable via `pub mod`.
- REFUTED: Kde backend — see REFUTED #3.

### 10. plugins-devices (20 CONFIRMED / 8 LIKELY + 1 UNCERTAIN — all survive)
- `plugins/lights/src/znp/error.rs:4` `ZnpError` (whole file) — 3 in-file hits only; every fallible path uses anyhow; no `use ... error::` anywhere. `:4` `SAPI` + `:35-40` `mod sapi` — no code sends subsystem 0x06 (SREQ/AREQ engine uses SYS/AF/ZDO/UTIL only). `:18` `af::INCOMING_MSG` — AF data-receive path never implemented (run_event_loop controller.rs:147-158 handles only `ZDO_END_DEVICE_ANNCE_IND`). `:29` `zdo::MGMT_PERMIT_JOIN_RSP` — only REQ side live (coordinator.rs:105 via permit_join chain). `znp/coordinator.rs:47` `NetworkInfo` — return type of `startup`(:54)/`get_device_info`(:81); result dropped at controller.rs:69, `pan_id`/`channel` hard-coded 0 (:96-97); no serde derives; change both to `Result<()>`. `:34` `impl Default for NetworkConfig` — zero `::default()` hits; only explicit literal (controller.rs:64). `znp/transport.rs:22` `impl Default for TransportConfig` — zero `::default()` hits; two explicit literals (controller.rs:56, dongle.rs:37). `:75` `Transport::receiver()` — `.receiver()` zero hits in plugins/lights; consumers use `send`/`recv` only. `znp/device.rs:69` `DeviceRegistry::devices_mut()` — definition-only; mutations via `register` (controller.rs:136,181,196). `znp/mod.rs:18` `pub use zcl::ZclFrame` — all consumers use `super::zcl::ZclFrame`/`crate::znp::zcl`; the other four same-line re-exports are consumed. `service/light_service.rs:15` `LightService::health()` — `.health()` only wrapper body + trait impl; daemon emits hard-coded JSON (daemon/mod.rs:169-182); `BackendHealth` literal (zigbee/mod.rs:66) reached only from dead `health()`. `:19` `LightService::list_targets()` — only the 3 chain sites; live query is `list_devices` (qol-config.toml). `backend/mod.rs:8-10` `LightBackend::{kind, health, list_targets}` — `.kind()` zero hits on a backend value; the UI/doctor `"kind"`/`"backend_kind"` strings read the LIVE serde field `BackendConfig.kind` (config/model.rs:53) — keep the config field, remove only the trait methods. `domain/model.rs:17` `LightTarget::main_device()` + `:37` `LightCapabilities::rgb_cct()` + `:96` `LightTargetInfo::main_rgb_cct()` + `:122` `BackendHealth::degraded()` — each referenced only by the dead list_targets chain; production constructions are literals. `domain/model.rs:79` `Preset` struct — zero constructions; every other `Preset` hit is PresetConfig/PresetSlot/theme presets/plugin.toml labels (different types). `daemon/state.rs:47` `DaemonState::config()` — `.config()` zero hits in plugins/lights; keyremap tap.rs:278 is a different crate's type; the `config` FIELD is written/read internally. `plugins/bluetooth/src/lib.rs:8` `pub use bluetooth::retry;` — `crate::bluetooth::retry` (linux.rs:28) resolves via `bluetooth/mod.rs:7 pub mod retry`, bypassing the alias; `plugin_bluetooth::retry` appears nowhere; `pub mod retry` stays, only the alias goes. Search terms as in original (each verified with independent greps by the adversary).
- LIKELY (8, no refutation): `znp/zcl/color.rs:3` `move_to_hue_sat` (own test only; standard ZCL 0x06 — keep if hue/sat planned); `znp/device.rs:61` `DeviceRegistry::remove()` + `:73` `by_network_address()` (test-only; production via `by_ieee_address`); `backend/zigbee/mod.rs:17` `state_cache` (never written; read only in dead `list_targets`; only hook for a future live-state report); `domain/model.rs:57` `LightState` + `:88` `LightTargetInfo` + `:108` `BackendConnectionStatus::{Disconnected, Connecting, Degraded}` (only `Connected` ever constructed; enum stays as field type of `BackendHealth.status`); `daemon/mod.rs:75` `DeviceLeft` match arm (pairs with UNCERTAIN variant).
- UNCERTAIN (no refutation): `znp/controller.rs:44` `ZigbeeEvent::DeviceLeft([u8; 8])` — never constructed (run_event_loop sends only DeviceJoined, controller.rs:209); the deliberate daemon match arm signals device-leave intent; low runtime risk, human judgment needed. MOST DANGEROUS in slice: not mechanical, encodes planned intent.
- Date correction: scaffolding landed 2026-03-24 (see Corrections).

### 11. plugins-misc (7 CONFIRMED / 2 LIKELY → 1 survive, 1 refuted; 1 UNCERTAIN stands)
- `plugins/qol-shot/src/capture/screenshot.rs:175` `capture_for_preview()` — 1 hit (definition); daemon live path is `freeze_frame` + `capture_selected_for_preview` (app/mod.rs:223,237); gate string `"cli-preview-capture"` zero refs; no crate depends on the qol-shot lib. `capture/geometry.rs:147,155,164` `BackdropCorners`/`BackdropRegions`/`backdrop_regions()` — only re-export (lib.rs:9) + definitions + `#[cfg(test)]` tests (:393-551); region selector has its own `backdrop_segments` (region_selector/mod.rs:1362). `lib.rs:8` `pub use app as daemon_app;` — plan-doc hits reference a historical `src/daemon_app.rs` FILE that no longer exists; `qol_shot::app` live (main.rs:17). `plugins/removeapp/src/core/mod.rs:162` `quit_app(app)` — zero callers; CLI `--quit` → `quit_and_wait` (cli/mod.rs:304-305), UI `try_quit` → `quit_and_wait` (ui/mod.rs:151-163); the plan doc shows the UI was DESIGNED for `core::quit_app` but shipped with `quit_and_wait` — historical artifact; underlying `platform().quit` trait method stays. `:207` `search(query)` — zero callers; UI searches via `filter` + `installed_apps` (ui/mod.rs:70), CLI via `resolve_unique`; `search_box` (ui/mod.rs:360) is an unrelated UI method. `core/platform/macos/mod.rs:44` `with_roots(home, app_dirs)` — 4 uses all inside `#[cfg(test)]` (:446,502,605,638); Linux equivalent is cfg(test)-gated at fn level (linux/mod.rs:93-94) — move into the test module; the fn itself must survive for macOS tests. window-actions inert `pub` on crate-private modules (`restore/mod.rs:5,8,17,26,42,71`, `restore/state_store.rs:9,14`, `config/mod.rs:16,27`, `platform/linux/system.rs:12,162,174`, `platform/macos/mod.rs:84`, `diagnostics/mod.rs:9,20` ~20 sites) — bin-only crate, no `[lib]`; removing `pub` is semantically inert hygiene; the items themselves are all referenced internally and stay.
- LIKELY survivor: `plugins/removeapp/src/cli/mod.rs:181` `legacy_cask_json()` + `"cask"` key at :217 — deliberately named `legacy_*` (old-output back-compat); design-spec output contract (2026-06-21 design:134) omits `cask`; no in-repo consumer; external CLI/script consumers possible — human check before removal.
- UNCERTAIN (no refutation, precision correction): `plugins/window-actions/src/restore/mod.rs` + `state_store.rs` `saved_rect` chain — the RESTORE FEATURE is fully live and host-exercised (`[action.restore]` plugin.toml:36-38, qol-config.toml:113, cli.rs:14+121, app/mod.rs:200, platform handlers, doctor `restore_state`, qol-cli emu workflow drives minimize/restore at window_actions/platform/linux.rs:315-348); only the RECT PLUMBING is inert (`window_rect` Linux always None at linux/system.rs:44-46; `saved_rect` field restore/mod.rs:13; `restore_rect` Linux no-op :99-101, macOS parses-then-drops macos/mod.rs:150-154). Removing the rect plumbing does not touch live restore behavior; gates: qol-tray-data-migrations policy + on-disk record format (`parse_record` state_store.rs:101 already optional).
- REFUTED: `Command::Kill`/`"kill"` — see REFUTED #4.

### 12. tools-cli (1 CONFIRMED / 2 LIKELY — all survive)
- `tools/qol-cli/Cargo.toml:29` `qol-platform.workspace = true` — unused `qol` crate dep; `qol_platform|qol-platform` under tools/qol-cli → only the manifest line; git history: last qol-cli usage moved out to `libs/qol-dev-env/src/resources.rs:507` in 2b9a8dbc (2026-07-15), the dep line left behind; no build.rs, no `[features]`. Removing the line drops qol-platform (+libc/zbus/dirs edges) from the `qol` build graph only. NOTE: "removal compile-proven" claim unverifiable (artifacts missing — see Corrections); the textual evidence stands.
- LIKELY (2, no refutation): `tools/qol-cli/src/dev_console/feature_flags.rs:13` `enum FeatureFlag {}` + `:21` `FEATURE_FLAGS = &[]` + `:64` `toggle_feature_flag` + whole subsystem (`FeatureFlags`/`FeatureFlagPanel`, `draw_feature_flags_panel` + `feature_flag_brick_*` :68-174, `edit_feature_flags` session.rs:343-351, `Action::FeatureFlags` + ctrl+f binding key_bindings.rs:153-157,465-467, `dash.features`/`dash.feature_panel` dash.rs:369-370,440-443,548,558,658, `ConsoleState.feature_flags` console_state.rs:20, tests session.rs:1027-1056) — uninhabited enum, empty registry since the module split (0fa34f9b/6d73a04b); all 59 grep hits are dev_console internals; MOST DANGEROUS LIKELY: live wired UI (ctrl+f → panel), designed empty-state affordance, cited as the reference pattern for the worktrees panel (spec 2026-07-04-compile-to-worktree-cli-design.md:76). `apps/qol-tray/skills/qol-cli-commands/SKILL.md:13-16` + `:128` stale doc refs (`src/cli.rs` → actual `src/cli/mod.rs`+`contract.rs`; `src/dev_console.rs` → actual `src/dev_console/`; `src/commands/emu.rs` → actual `emu/`; `:128` "leaves-no-trace" → actual 12 workflows, workflow/mod.rs:159-208) — documentation drift; confirm shape before editing since the skill auto-loads.

### 13. cross-cutting-deps (4 CONFIRMED / 1 UNCERTAIN → 4 survive, 1 refuted)
- `tools/qol-cli/Cargo.toml:29` → `qol-platform` (CONFIRMED; see slice 12). `libs/qol-dev-guest/Cargo.toml:14` → `tempfile = "3.8"` dev-dep (CONFIRMED; crate = src/lib.rs only; test module :527+ uses `std::net::TcpListener`/`std::thread`; qol-cli workspace.rs:225 tempfile use is a different crate). `Cargo.toml:41` → `qol-tray = { path = "apps/qol-tray" }` workspace line (CONFIRMED; zero member refs in all tables incl. target.cfg; Cargo.lock confirms no package lists it as a dep; the manifest_sanitizer.rs:98 string is a test fixture for stripping dev-deps from downloaded plugin manifests — different string, not a consumer; libs/qol-workspace/src/lib.rs:112 treats "qol-tray" as a directory name; githooks never parse `[workspace.dependencies]`; qol-migrate bake script member pairs are tree-comparison only). `Cargo.toml:29` → `qol-wasm = { path = "libs/qol-wasm" }` workspace line (CONFIRMED; only the line is dead — the crate stays, cdylib source of the live committed bundle imported by `apps/qol-tray/ui/components/CommandPalette.js:5`; no wasm-pack regen step exists per simplification-findings.md:43).
- REFUTED: `local-stt` feature — see REFUTED #5.
- KEEP-verified live (not candidates): workspace `objc2*`/`raw-window-handle`/`x11rb` entries (consumed via `[target.'cfg(...)'.dependencies]`), `[profile.sandbox]` (Cargo.toml:79-84, invoked by flow.rs:2605-2606), tray features `dev`/`linux_evdev`, launcher `examples` feature, `[workspace.package]` fields, both vendor patches (global-hotkey, ravif, core-* git patches — all reachable in Cargo.lock).

### 14. cross-cutting-duplication (16 CONFIRMED → 14 survive, 2 downgraded; 13 LIKELY + 2 UNCERTAIN survive)
- SURVIVES (14): alt-tab `runtime/daemon.rs:38-52` ≡ launcher `app/mod.rs:38-51` `parse_command` + wrapper trio (read-verified identical; deltas exactly as cited: `send_show` expect_reply, `support_replace_existing`, alt-tab's show-reverse/open-reverse; `CMD_RECV` → 7 hits / 6 files); removeapp `daemon/actions.rs:19-26` ≡ cli-sessions `daemon/actions.rs:18-27` `parse_command` (identical skeleton; cli-sessions adds next/snapshot; CONFIG differs only in socket source; NOTE the "identical trio" attribution to these two files is wrong — neither has send_kill/cleanup); lights `daemon/state.rs:17-22` `DaemonOutcome` ≡ `ReadResult<C>` (exact variant shapes; converters at daemon/mod.rs:122-133,220-228; 50 hits confined to lights daemon/{mod,state}.rs; deletion path `handle_action → ReadResult<()>` sound); exec-bit predicates (cli-sessions doctor/unix.rs:4-14 ≡ bluetooth linux.rs:88-98; qol-shot system.rs:168-192 ≡ window-actions doctor.rs:100-124; `0o111` → 14 files; CAVEATS: bluetooth does NOT depend on qol-fs — add dep or retarget to qol-runtime; the 14-file sweep includes variants — qol-shot macos swift.rs:89 adds `&& metadata.uid() == current_uid()`); pointz `string_to_key` linux.rs:313-394 ≡ macos.rs:549-630 + windows.rs:544-625 `string_to_vk` (byte-identical; CAVEAT: pointz does NOT depend on qol-hotkeys — would be a new dep); keyremap platform/{fallback,linux,windows}/mod.rs (43 lines, only `name()` differs); controllers platform/{macos,windows}.rs (26 lines, only label differs); `valid_install_id` trio (libs/qol-config/src/lib.rs:316-322, tray paths/mod.rs:112-118, doctor/install_id.rs:44-50 — identical predicate; lib copy is the extracted home 659afdd0; both tray copies deletable); cloexec trio (qol-plugin-daemon unix.rs:428-438, tray listener/unix.rs:228-238, lifeline_handoff/unix.rs:89-102 — same fcntl wrapper; `clear_cloexec` runs inside `Command::pre_exec` (listener/unix.rs:224) — shared helper must stay `FnMut + Send + io::Result`-compatible, lib copy already is); tray `stop()` unix_common.rs:110-130 vs lib `replace_existing` unix.rs:377-399 (kill-then-poll; constants 1s/10ms vs 2s/20ms; tray already delegates send_kill/send_ping — only the poll loop duplicates); tray `bind_reclaiming_stale_socket` listener/unix.rs:157-169 vs lib `bind_listener` AddrInUse arm unix.rs:326-375 (same reclaim over `local_ipc::bind_listener`; probe differs); daemon-mode gate trio (controllers main.rs:4-13 ≡ qol-voice main.rs:4-15 ≡ bluetooth main.rs:14-22; `args.is_empty() && var_os(ENV_DAEMON_SOCKET)` exactly 3 files; qol-shot/lights variants deliberately excluded); qol-shot `config/settings.rs:5-8` `open_qol_settings` vs `qol_apps::desktop_integration::open_plugin_settings` (all three `open_url` bodies already delegate to `open_with_default_app` — `open_qol_settings` is exactly `open_plugin_settings(PLUGIN_ID)` + context; qol-shot deps qol-apps ✓, caller cli.rs:133); tray daemon_tracker/registry.rs:16-30 `tracked_pids` vs qol-cli dev_shutdown/platform/pid_files.rs:12-38 `tracked_pids_from_dir` (identical scan; both live — tray census.rs:9, qol-cli own test pid_files.rs:44-69; qol-cli deps qol-fs ✓). Consolidation targets as in original table: qol-runtime::local_ipc (framing only), qol-plugin-daemon DaemonHandle, qol-fs, qol-hotkeys, shared stub modules, qol-config, qol-plugin-daemon (parameterized), qol-plugin-api, qol-apps, qol-fs.
- LIKELY (13, no refutation): run-loop glue (keyremap macos app/mod.rs:23-58, pointz app/mod.rs:34-61, os-themes app/daemon_run.rs:8-42, alt-tab runtime/operational.rs:59-76 — exit semantics differ, parameterize); command-bridge thread (bluetooth linux.rs:509-519 vs qol-shot app/mod.rs:104-114 — different sender types); DaemonResponse→Result mapping (bluetooth linux.rs:467-489 vs qol-voice app/mod.rs:41-50); `execute_action_once` (controllers app/mod.rs:332-346 vs lights daemon/mod.rs:33-37); `is_safe_id` vs `validate_safe_identifier` (qol-migrations fs_util.rs:89-96 vs qol-plugin-api command_rules.rs:40-60 — whitespace delta deliberate); window-actions `execute_action` (linux/mod.rs:21-51 vs macos/mod.rs:56-78 — vocabulary identical, bodies differ, macOS extra `screens`); os-themes `apply_gtk_theme` vs `apply_shell_theme` (gnome.rs:33-46 vs cinnamon.rs:31-44); lights platform/linux.rs:6-71 vs macos.rs:6-86 (five wrappers, per-OS heuristics differ); pointz doctor/mod.rs:201-215 `runtime_endpoints_result` + :251-258 `inherited_fd_present` (could be `inherited_port_fd(name).is_some()`); qol-shot main.rs:31-40 + lights runtime/mod.rs:13-19 daemon-gate variants (pinned/tested, deliberate); `runtime_pids_dir` (tray paths/mod.rs:287-289 vs qol-cli pid_files.rs:6-10); unsupported-platform stubs (os-themes settings/{macos,windows}.rs, theme/{macos,windows}/mod.rs, bluetooth platform/{macos,windows}.rs — byte-identical except OS-name strings).
- UNCERTAIN (2, no refutation): macOS CGEventTap FFI (tray hotkeys/capture/platform/macos/mod.rs:120-146 vs keyremap platform/macos/tap.rs:74-180 — deliberate divergence: passive HID capture + re-enable guard vs mutating HeadInsert that exits on failure; `CGEventTapCreate|CGEventTapEnable` exactly these two files; macOS-only, merge-risk judgment stays with a human); per-user runtime socket-path composition 4× (qol-runtime client/mod.rs:27-38, tray dev_generation/mod.rs:73-83, qol-runtime broker/path/platform/unix.rs:8-21, qol-plugin-daemon unix.rs:47-77 — each coupled to its own protocol; not consolidation-ready).

## Duplication consolidation targets worth doing (not removal candidates)

Original cross-cutting-duplication recommends these as follow-up refactors once/if shared-lib consolidation is funded; the highest-confidence targets (both copies live, target lib already a dep): `valid_install_id` → qol-config; tracked_pids scan → qol-fs; open_qol_settings → qol-apps; keyremap/controllers unsupported stubs → shared module; pointz string_to_key → qol-hotkeys (needs new dep); DaemonOutcome → qol-plugin-daemon (needs human review of converters). The two DOWNGRADED clusters (wire protocol, 12-daemon skeleton) need the parameterization/behavior decisions documented above before any consolidation.

## End-of-campaign state

131 CONFIRMED → 117 deletable now, 13 gated behind prerequisites, 1 (root_has_exited) promoted to DO-NOT-REMOVE. 7 items proven live by the adversarial pass (2 of which — Kde, local-stt — were framed as "low-impact" or "dormant" by the original pass and would have caused test failures or broken CI). 8 UNCERTAIN items remain human-judgment calls with documented reasoning. Every surviving item above carries its real path:line and the original search terms; no findings were invented or dropped in this merge.
