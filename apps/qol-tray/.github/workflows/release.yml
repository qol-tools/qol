name: Build and Release

on:
  push:
    tags:
      - "v*"

permissions:
  contents: write

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: false

jobs:
  build_linux_deb:
    name: Build Linux .deb
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: actions/checkout@v4
        with:
          repository: qol-tools/qol-config
          path: qol-config
          token: ${{ secrets.GITHUB_TOKEN }}
      - name: Link qol-config as sibling
        shell: bash
        run: |
          if [ ! -e ../qol-config ]; then
            ln -s "$GITHUB_WORKSPACE/qol-config" ../qol-config 2>/dev/null || cp -r "$GITHUB_WORKSPACE/qol-config" ../qol-config
          fi

      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Cache cargo
        uses: Swatinem/rust-cache@v2
        with:
          cache-targets: true
          cache-all-crates: true

      - name: Install cargo-deb
        uses: taiki-e/install-action@v2
        with:
          tool: cargo-deb

      - name: Install Linux dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y libgtk-3-dev libayatana-appindicator3-dev libxdo-dev

      - name: Build Debian package
        run: cargo deb

      - name: Prepare artifacts
        shell: bash
        run: |
          mkdir -p dist
          cp target/debian/*.deb dist/
          echo "Built artifacts:"
          ls -lh dist/

      - name: Upload artifacts
        uses: actions/upload-artifact@v4
        with:
          name: linux-deb
          path: dist/*.deb
          if-no-files-found: error

  build_macos:
    name: Build macOS bundle (${{ matrix.name_suffix }})
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: macos-latest
            target: aarch64-apple-darwin
            artifact_name: qol-tray-macos-aarch64
            name_suffix: Apple Silicon
          - os: macos-latest
            target: x86_64-apple-darwin
            artifact_name: qol-tray-macos-x86_64
            name_suffix: Intel

    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: actions/checkout@v4
        with:
          repository: qol-tools/qol-config
          path: qol-config
          token: ${{ secrets.GITHUB_TOKEN }}
      - name: Link qol-config as sibling
        shell: bash
        run: |
          if [ ! -e ../qol-config ]; then
            ln -s "$GITHUB_WORKSPACE/qol-config" ../qol-config 2>/dev/null || cp -r "$GITHUB_WORKSPACE/qol-config" ../qol-config
          fi

      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ matrix.target }}

      - name: Cache cargo
        uses: Swatinem/rust-cache@v2
        with:
          cache-targets: true
          cache-all-crates: true

      - name: Build macOS binaries
        run: cargo build --release --target ${{ matrix.target }} --bin qol-tray --bin qol-tray-install

      - name: Prepare artifact
        shell: bash
        run: |
          BUNDLE="${{ matrix.artifact_name }}"
          mkdir -p "$BUNDLE"
          cp target/${{ matrix.target }}/release/qol-tray "$BUNDLE/qol-tray"
          cp target/${{ matrix.target }}/release/qol-tray-install "$BUNDLE/qol-tray-install"
          chmod +x "$BUNDLE/qol-tray" "$BUNDLE/qol-tray-install"
          cat > "$BUNDLE/install.sh" <<'EOF'
          #!/usr/bin/env bash
          set -euo pipefail
          DIR="$(cd "$(dirname "$0")" && pwd)"
          "$DIR/qol-tray-install" --source "$DIR/qol-tray"
          EOF
          chmod +x "$BUNDLE/install.sh"
          cat > "$BUNDLE/README.txt" <<'EOF'
          Run ./install.sh from this folder.
          EOF
          tar -czf "${BUNDLE}.tar.gz" "$BUNDLE"

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.artifact_name }}
          path: ${{ matrix.artifact_name }}.tar.gz
          if-no-files-found: error

  release:
    name: Create GitHub Release
    needs: [build_linux_deb, build_macos]
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Download artifacts
        uses: actions/download-artifact@v4
        with:
          path: artifacts

      - name: Create release
        uses: softprops/action-gh-release@v1
        with:
          tag_name: ${{ github.ref_name }}
          name: QoL Tray ${{ github.ref_name }}
          generate_release_notes: true
          files: artifacts/**/*
          fail_on_unmatched_files: true
          allow_updates: false
          draft: false
          prerelease: false
