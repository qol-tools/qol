name: lint

on:
  push:
    branches: ["main"]
  pull_request:
    branches: ["main"]

jobs:
  lint:
    uses: qol-tools/qol-cicd/.github/workflows/lib-lint.yml@main
    with:
      system_deps: "libxkbcommon-dev libxkbcommon-x11-dev libxcb1-dev"
