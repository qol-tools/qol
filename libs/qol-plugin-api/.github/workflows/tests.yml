name: tests

on:
  push:
    branches: ["main"]
  pull_request:
    branches: ["main"]

jobs:
  tests:
    uses: qol-tools/qol-cicd/.github/workflows/lib-tests.yml@main
    with:
      system_deps: "libxkbcommon-dev libxkbcommon-x11-dev libxcb1-dev"
