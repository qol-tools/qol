[package]
name = "qol-cache"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Generic TTL cache with per-key checked_at, expiry on read, and prune on insert"

[lints.rust]
unsafe_code = "forbid"
