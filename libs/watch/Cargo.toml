[package]
name = "qol-watch"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Filesystem change notification primitives for qol-tools"

[dependencies]
notify = { version = "8", default-features = false, features = [
  "macos_fsevent",
] }
workspace-hack.workspace = true

[dev-dependencies]
tempfile = "3"

[lints.rust]
unsafe_code = "forbid"
