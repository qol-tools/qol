# plugin-kitty

Terminal lifecycle plugin for qol-tray. Owns kitty IPC, named workspace
lifecycle, snapshot+restore of pane layout, and the user-owned template
registry that gates which programs may run after a workspace reboot.

See `docs/adr/` for design records. Part of the terminal-workspace-restore
epic tracked at qol-tools/qol-tray#31.
