schema_version = 1

[action.apply_fixes]
description = "Apply driver fixes for connected known-broken controllers"

[query.controllers_status]
description = "Aggregate verdict across connected controllers"
poll_interval_ms = 3000

[query.list_controllers]
description = "Connected controllers with per-pad verdicts"
poll_interval_ms = 3000
