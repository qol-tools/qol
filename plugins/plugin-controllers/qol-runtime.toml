schema_version = 1

[action.apply_fixes]
description = "Apply driver fixes for connected known-broken controllers"

[query.controllers_status]
description = "Aggregate fix state across detected known controllers"
poll_interval_ms = 3000

[query.list_controllers]
description = "Detected known controllers with per-pad fix state"
poll_interval_ms = 3000
