use anyhow::{bail, Result};

pub(crate) fn run_panel() -> Result<()> {
    bail!("plugin-bluetooth native settings are not implemented on macOS")
}

pub(crate) fn spawn_panel() -> Result<()> {
    bail!("plugin-bluetooth native settings are not implemented on macOS")
}
