[plugin]
id = "plugin-launcher"
name = "Launcher"
description = "Universal search with action modifiers"
version = "1.12.1"
author = "KMRH47"
platforms = ["linux", "macos"]

[runtime]
command = "launcher"

[action.open]
label = "Open Launcher"
args = ["--show"]

[daemon]
enabled = true
command = "launcher"
socket = "/tmp/qol-launcher.sock"

[capabilities]
gpui = true

[menu]
label = "Launcher"
items = []

[[dependencies.binaries]]
name = "launcher"
repo = "qol-tools/plugin-launcher"
pattern = "launcher-{os}-{arch}"
