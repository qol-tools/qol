[package]
name = "gpui-test"
version = "0.1.0"
edition = "2021"

[dependencies]
gpui = { version = "0.2", features = ["test-support"] }
gpui-component = "0.5.0"

[dev-dependencies]
proptest = "1.0"
rand = "0.8"
